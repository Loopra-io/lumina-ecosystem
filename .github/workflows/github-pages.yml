name: Legacy GitHub Pages workflow

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: echo "Deployment is handled by the static workflow."
